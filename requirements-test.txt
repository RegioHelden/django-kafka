-r requirements-ci.txt
confluent-kafka[avro, schemaregistry]==2.13.2
django==6.0.4
django-temporalio==2.0.0
ruff==0.15.10
setuptools==82.0.1  # without it PyCharm fails to index packages inside the Docker container
